name: Nightly - testnet

on:
  workflow_dispatch:

jobs:
  testnet:
    uses: ci-base.yml
    with:
      network: testnet
      allure_upload: true
      allure_project_id: midnight-js-testnet
      yarn_it: 'yarn it --cache-dir ./turbo --force'
      build_timeout: 180
    secrets: inherit
