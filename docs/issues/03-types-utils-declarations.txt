TITLE: feat: keep-state declarations in types + runtime helpers in utils

CONTEXT
Architecture doc section 3.2 (types/utils rows of the providers table, the
KeepStateBridge interface under contracts, the ".tx typing decision" note) and
section 5 (data & type boundaries). MUST land before the compat package —
compat implements the types-declared interface via its peerDependency
(section 9, delivery sequence). types stays declarations-only; runtime code
goes to utils.

SCOPE — types (consumer-compile-compatible, declarations ONLY)
- KeepStateBridge<TArgs, TWitnesses, TPrivateState>:
    usesSameLedgerInstance(probe: typeof ContractState): boolean
      (probe = the ContractState constructor from protocol/ledger — pinned
       symbol, typed parameter; wrong symbol = compile error)
    executeCall(input: KeepStateCallInput<...>): Promise<KeepStateCallResult<...>>
  Witness/private-state shapes are OPAQUE GENERICS — closing OQ13 must not
  churn this surface (section 10 gating table).
- KeepStateCallInput { serializedContractState: Uint8Array; circuitId; args;
  witnesses; privateState } / KeepStateCallResult { transcript; callPrototype:
  ContractCallPrototype; nextPrivateState }.
- Keep-state entry options type (the keepState field of the dedicated entry —
  section 3.3, injection point).
- FinalizedTxData.rawTx?: Uint8Array — the SOLE additive record field
  (protocolVersion pre-exists). Declared tx stays REQUIRED — the union was
  consciously rejected as consumer-compile-breaking; see ".tx typing decision"
  in section 3.2.
- Two provider members on PublicDataProvider: queryRawContractState,
  queryLatestProtocolVersion — implementer-facing breaking; policy: all
  in-repo implementations + testkit mocks update in the provider PR (ticket 08).

SCOPE — utils (runtime)
- isDecodedTxData(d): d is FinalizedTxData & { readonly rawTx?: undefined }
  (narrowing on rawTx ABSENCE).
- createRawFinalizedTxData(fields): installs the NON-ENUMERABLE throwing tx
  accessor + populates rawTx. Mandatory for provider and testkit mocks — an
  object literal silently satisfies the interface without the throw.

TESTS
- Consumer-compile-compat assertion (existing consumer code compiles unchanged).
- Guard narrows correctly both ways; copy semantics (QA-6, section 4.3): after
  spread/clone tx === undefined (NO throw), rawTx byte-equal,
  isDecodedTxData(copy) === false.
- Descriptor-parity test: records built via the factory carry identical property
  descriptors for tx (basis for provider/mock parity in 08).
- JSON.stringify / spread / structuredClone / deep-equality of a raw record do
  NOT throw; direct .tx read DOES throw the typed error naming the compat codec
  (section 7 table).

ACCEPTANCE
- types diff is declarations-only; the three consumer-runtime caveats from the
  section 3.2 types row are recorded in code docs (.tx runtime break, SEC-9
  pre-fork throw, implementer-facing provider members).

ESTIMATE: M (2-3 days)
DEPENDS ON: 02
BLOCKS: 05, 06, 07, 08
