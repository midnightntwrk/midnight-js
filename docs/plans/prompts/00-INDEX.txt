HF v8/v9 DUAL SUPPORT — PROMPTS FOR NEXT CLAUDE INSTANCES
Generated 2026-08-17; refreshed 2026-09-04 against the integration tip a09a6801.
Each live prompt is self-contained: paste one file's content as the first message
of a fresh Claude Code session.

THE BASE, BEFORE ANYTHING ELSE (owner ruling 2026-09-04)
  All remaining hard-fork work branches off `origin/feat/1004-protocol-dual-ledger`
  and every PR targets that branch. PR #1218 tracks it but does NOT have to reach
  `main` first — it is a long-living integration branch, not a pending merge. This
  is a deliberate exception to the repo's usual "branch from clean origin/main"
  rule and it holds for the whole fork window. Sync parent -> child by MERGE,
  never rebase (a rebase orphans the merge bases the stack depends on), and give
  each stacked child its own yarn.lock commit whenever the root `resolutions` pin
  moves.

  Branching off `main` would give you a tree with none of what these plans extend.
  Verified 2026-09-04: `main` has ADRs 0001-0005 only, no `packages/protocol/docs/`
  directory, and zero occurrences of `unwrapV9`, `loadLedger8`,
  `queryRawContractState` or `requireV9Era`.

ORDER AND STATUS
  01-oq13-discovery.txt      — DELIVERED (spike results folded into spec v5.2)
  02-fixture-port.txt        — DELIVERED (fixtures live in
                               testkit-js/testkit-js/src/fixtures/hf/)
  03-mjs01-engine-resume.txt — DELIVERED (MJS-01 stack landed on the integration
                               branch: #1155, #1156, #1164, #1168, #1165, #1194,
                               #1198)
  04-types-d14.txt           — DELIVERED as #1204 -> #1177 -> #1207, except the AC7
                               API-report gate, which is still owed (spec §10 step 2)
  05-mjs02-contracts.txt     — LIVE. Start here or at 06; they are independent
                               except where Task 4.4 and Task 3.5 must lift the v8
                               seam refusal together.
  06-mjs03-providers.txt     — LIVE. Tasks 4.1/4.2 are done; the prompt starts at 4.3.
  07-pre-merge-checklist.txt — USER actions. Largely spent: OQ2 was closed by the
                               owner on 2026-09-03, and the "before merging #1218"
                               framing no longer applies now that #1218 is a base
                               rather than a pending merge.

STATE SNAPSHOT (2026-09-04, re-verify live via gh/git before acting)
  - Integration branch `feat/1004-protocol-dual-ledger` @ a09a6801 carries the whole
    MJS-01 stack, the #1006 type surface (#1204 -> #1177 -> #1207) and #1225.
  - #1225 REMOVED the corroborated era latch. `queryLatestProtocolVersion()` takes
    no arguments; `IndexerPublicDataProvider` issues one uncached head request per
    call; the interface permits a cache only if it EXPIRES BY ITSELF. Recorded as
    ADR 0008. Do not reintroduce corroboration — spec D16 is reversed at v5.5.
  - Spec is at v5.5, on this branch:
    docs/specs/2026-07-09-ledger-v8-v9-dual-support-design.md
  - Worktrees live under <repo>/.worktrees/<branch-with-dashes>. Any absolute path
    of the old `midnight-js.<branch>` shape is dead; prompts 01-03 still carry some.

HARD RULES REPEATED IN EVERY LIVE PROMPT (do not drop them when editing prompts)
  - Every code change on a NEW wt worktree branched from clean
    origin/feat/1004-protocol-dual-ledger (see THE BASE above); EnterWorktree;
    yarn install && yarn build before first push — pre-push lint resolves subpath
    exports against dist.
  - NEVER cite the design spec (OQ*, §*, D*, file name) in code, comments, JSDoc or
    error messages — it is not tracked on any code branch. Cite instead ADR 0004,
    0006, 0007, 0008 and the nine documents under `packages/protocol/docs/`: those
    travel in the SAME TREE as the code citing them. Pre-existing violations exist
    in indexer-public-data-provider and utils/deserialization — do not add more.
  - Error codes: prefix MIDNIGHT_JS_<layer>_ (P/C/PR/U); classes local to the
    raising package; codes registered in packages/utils/src/error-codes.ts (protocol
    codes imported from @midnight-ntwrk/midnight-js-protocol/errors leaf subpath,
    never the barrel); every new code negative-tested (AC2). The registry test's
    expectation list is hand-spelled and has gone stale once — derive it.
  - ESLint gate: a runtime import of @midnight-ntwrk/midnight-js-protocol/v8 or
    /engine is banned outside packages/protocol/src (type-only imports are allowed).
  - TDD; no `any` / `as unknown`; Apache 2.0 header on new .ts/.sh; conventional
    commits, GPG; never push without a green pre-push; subagents never post to
    GitHub and never push.
  - Do not auto-commit anything under docs/ plans/specs without explicit user
    approval.
