HF v8/v9 DUAL SUPPORT — PROMPTS FOR NEXT CLAUDE INSTANCES
Generated 2026-08-17 from the live session state. Each prompt is self-contained:
paste one file's content as the first message of a fresh Claude Code session
started in /Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js.

ORDER AND DEPENDENCIES
  01-oq13-discovery.txt      — can start NOW (parallel to everything; blocks 03 tasks 1.7/1.8 and 05 task 3.2)
  02-fixture-port.txt        — can start NOW (blocks 03 tasks 1.5/1.6)
  03-mjs01-engine-resume.txt — after 02 (tasks 1.5/1.6) and additionally 01 (tasks 1.7/1.8)
  04-types-d14.txt           — after PR #1155 and #1156 merge to main
  05-mjs02-contracts.txt     — after 04's D14-B PR and the cross-plan provider-members PR merge
  06-mjs03-providers.txt     — cross-plan PR together with 04; rest after 04
  07-pre-merge-checklist.txt — USER actions (not a Claude prompt): OQ2 scope check etc.

STATE SNAPSHOT AT GENERATION TIME (always re-verify live via gh/git!)
  - PR #1155 feat/1004-version-module-gates (base main): version module, utils
    error registry + tag parse, ESLint/cast gates. Full 5-agent review applied
    (head ba0b0c1a): hardened check-casts.sh, dynamic-import gate, typed
    registry unions, strict tag grammar {namespace,version}, reason-branched
    UnknownProtocolVersionError, protocol ./errors leaf subpath, TSDoc+READMEs.
  - PR #1156 feat/1004-v8-subpath-loadv8 (base = #1155 branch, STACKED): ./v8
    subpath + lazy loadV8 + dist gate + @midnightntwrk/ledger-v8@8.1.1. A
    parallel session applied its own review there (loadV8 wraps failures in
    Ledger8RuntimeMissingError + memo-clear retry; publish-script scope
    rewrite fix) — commits f00027f7, 4c48a992. NEEDS REBASE onto #1155 head
    ba0b0c1a (owned by worktree midnight-js.feat-1004-v8-subpath-loadv8).
  - ADR 0004 (lazy v8 era access) in separate PR #1157.
  - Plans live on branch docs/superpowers-specs (worktree
    midnight-js.docs-superpowers-specs): docs/plans/2026-08-17-hf-*.md; the
    spec is docs/superpowers/specs/2026-07-09-ledger-v8-v9-dual-support-design.md
    (v5.1 + §4.1(3) erratum: loadV8 = package self-reference import).
  - MJS-01 SDD ledger (tasks 1.1-1.4 complete): worktree
    midnight-js.feat-1004-protocol-dual-ledger,
    .superpowers/sdd/2026-08-17-hf-mjs01-protocol-plan/progress.md
    (+ oq3d-discovery-note.md: OQ3d answered — Block.protocolVersion via
    Query.block(offset:null); paste into spec §11 + issue #1006 if not done).

HARD RULES REPEATED IN EVERY PROMPT (do not drop them when editing prompts)
  - Every code change on a NEW wt worktree branched from clean origin/main
    (wt switch --create <branch> --no-cd; git reset --hard origin/main after
    create if local main lags; EnterWorktree; yarn install && yarn build
    before first push — pre-push lint resolves subpath exports against dist).
  - NEVER cite the design spec (OQ*, §*, D*, file name) in code, comments,
    JSDoc or error messages — the spec is not tracked on main. Inline the
    content or cite durable upstream sources (e.g. midnight-indexer
    protocol_version.rs). Pre-existing violations exist in
    indexer-public-data-provider and utils/deserialization — do not add more.
  - Error codes: prefix MIDNIGHT_JS_<layer>_ (P/C/PR/U); classes local to the
    raising package; codes registered in packages/utils/src/error-codes.ts
    (protocol codes imported from @midnight-ntwrk/midnight-js-protocol/errors
    leaf subpath, never the barrel); every new code negative-tested (AC2).
  - TDD; no `any` / `as unknown` (CI gate scripts/check-casts.sh is
    no-NEW-occurrences vs scripts/cast-baseline.txt); Apache 2.0 header on
    new .ts/.sh; conventional commits, GPG; never push without a green
    pre-push; subagents never post to GitHub and never push.
  - Do not auto-commit anything under docs/ plans/specs without explicit
    user approval.
