PRE-MERGE CHECKLIST — USER ACTIONS (not a Claude prompt)
Written 2026-08-17; refreshed 2026-09-04. Re-verify live before acting.

THERE IS NO PRE-MERGE MOMENT RIGHT NOW.
The original heading assumed PR #1218 would land on `main` next. The owner ruled
on 2026-09-04 that it need not: `feat/1004-protocol-dual-ledger` is a long-living
integration branch that MJS-02 (#1005) and MJS-03 (#1006) also build on, so there
is no merge to gate until the whole fork window is feature-complete. Two items the
owner still holds:

[ ] #1218's description no longer matches what the branch is. It still reads as an
    MJS-01 merge PR: a "Before merging" section prescribing the merge mechanics, an
    OQ2 checkbox that is already closed, and "Closes #1004 / Closes #1006" with no
    mention of #1005, which will land on the same branch. Decide whether to rewrite
    it now or when the window closes.
[ ] Whenever the window does close: merge with a merge commit, never a squash.
    Squashing would orphan the ancestors the stacked branches depend on.

SPENT — nothing to do:
[x] OQ2 credentialed scope check — CLOSED by the owner on 2026-09-03. Same
    maintainer account on both npm scopes, dual publishing intended, exact pins and
    lockfile checksums verified. The one item deliberately not treated as a
    blocker: no CI gate on audited scopes/versions exists, and it only becomes
    meaningful at the cutover to `@midnightntwrk`. Plan the gate with the cutover.
[x] Rebase #1156 onto #1155 — spent. GitHub's merge-stack collapsed all seven
    MJS-01 PRs (#1155, #1156, #1164, #1168, #1165, #1194, #1198) into one merge
    commit on the integration branch.
[x] Merge-order note for #1156 — spent with the same stack merge.
[x] Paste the OQ3d answer into the spec and issue #1006 — OQ3d is answered AND
    shipped: `HEAD_PROTOCOL_VERSION_QUERY` = `query { block { protocolVersion } }`,
    with a docker integration test asserting the provider against that exact field.

STILL OPEN:
[ ] OQ13 discovery outputs (prompt 01) → post the A4/A5 questions to the tkerber
    thread on #1005; record the rulings in the spec.
[ ] Owner assignments the spec requires before freezes: OQ8 (security, plus a
    formal interim risk acceptance with name and date before the first release),
    OQ12, OQ13, OQ14; the OQ18 scope ruling (connector-proving dApps) is a PO
    decision. OQ2 is off this list — closed 2026-09-03.
[ ] Pre-existing spec citations in code (they violate the no-spec-refs rule but
    predate it): indexer-public-data-provider/src/{get-all-contract-events,
    events-filter}.ts, utils/src/deserialization/{patterns,classify}.ts — schedule
    a small cleanup PR (inline the content, or cite ADRs / packages/protocol/docs,
    which are tracked with the code).
[ ] Deferred minors from reviews: packages/utils missing its own rollup/ts
    devDependencies (pre-existing; chore PR); prettier drift in
    eslint.config.mjs/version.ts (cosmetic); the protocol ESLint override sets
    rules fully 'off' (re-declare the intended exceptions for durability).
[ ] `nightly-e2e.yml` has been `startup_failure` with ZERO jobs launched for
    consecutive nights. It is the only CI coverage for eras other than v9 — it
    rotates TESTKIT_DOCKER_ENV across all five image sets. Until it runs, every
    e2e signal on this stack is devnet, i.e. node 2.x / ledger v9 only. Recorded
    on #1207; it blocks MJS-03 Task 5.3.
[ ] After the remaining PRs land, delete stale worktrees (`wt remove`) and the SDD
    workspace ledgers they contain — but ONLY once the resume value is gone
    (prompts 05 and 06 read those ledgers). Worktrees now live under
    <repo>/.worktrees/<branch-with-dashes>.
