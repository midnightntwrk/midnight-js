PRE-MERGE CHECKLIST — USER ACTIONS (not a Claude prompt)
State as of 2026-08-17; re-verify live before acting.

BEFORE MERGING PR #1155 + #1156:
[ ] OQ2 credentialed scope check (MUST-FIX-BEFORE-MERGE per final review):
    with npm registry credentials, confirm (a) no competing package
    `@midnight-ntwrk/ledger-v8` exists in the hyphenated scope, and (b) npm
    org ownership of `@midnightntwrk` is the expected organisation. The dep
    added in #1156 is `@midnightntwrk/ledger-v8@8.1.1` (exact pin, lockfile
    checksum verified) — chosen by elimination because the hyphenated scope
    was uncheckable from the dev environment (expired PAT). Record the result
    in the OQ2 checklist in the spec, incl. the 8.1.0→8.1.1 pin drift.
[ ] Rebase #1156 (feat/1004-v8-subpath-loadv8) onto #1155 head ba0b0c1a —
    the branch lives in worktree midnight-js.feat-1004-v8-subpath-loadv8
    (parallel session added f00027f7, 4c48a992 there). Expect a conflict in
    packages/protocol/src/errors.ts (both sides touched it: #1155 fix wave
    added reason-branching + freeze; #1156 added Ledger8RuntimeMissingError).
    Merge order: #1155 first, then retarget/merge #1156.
[ ] Merge order note: #1156's PR base is the #1155 branch — after #1155
    merges, retarget #1156 to main (GitHub does this automatically on branch
    delete, but verify).

HOUSEKEEPING (any time):
[ ] Paste the OQ3d answer into spec §11 OQ3(d) + a comment on issue #1006
    (draft: worktree midnight-js.feat-1004-protocol-dual-ledger,
    .superpowers/sdd/2026-08-17-hf-mjs01-protocol-plan/oq3d-discovery-note.md).
[ ] OQ13 discovery outputs (prompt 01) → post the A4/A5 questions to the
    tkerber thread on #1005; record rulings in the spec.
[ ] Owner assignments the spec requires before freezes: OQ8 (security,
    + formal interim risk acceptance name+date before first release), OQ12,
    OQ13, OQ14; OQ18 scope ruling (connector-proving dApps) is a PO decision.
[ ] Pre-existing spec citations in code (violate the no-spec-refs rule but
    predate it): indexer-public-data-provider/src/{get-all-contract-events,
    events-filter}.ts, utils/src/deserialization/{patterns,classify}.ts —
    schedule a small cleanup PR (inline the content or cite tracked docs).
[ ] Deferred minors from reviews (ledger in worktree
    midnight-js.feat-1004-protocol-dual-ledger): packages/utils missing own
    rollup/ts devDependencies (pre-existing; chore PR); prettier drift in
    eslint.config.mjs/version.ts (cosmetic); protocol ESLint override sets
    rules fully 'off' (re-declare intended exceptions for durability).
[ ] After the plan's remaining PRs land, delete stale worktrees (wt remove)
    and the SDD workspace ledgers they contain — but ONLY once the resume
    value is gone (prompts 03-06 read those ledgers).
