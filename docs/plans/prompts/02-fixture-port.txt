TASK: Port the HF spike fixtures into testkit (MJS-01 plan Task 0.2 = PR 1004-A).
Read first:
- Plan Task 0.2: /Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js.docs-superpowers-specs/docs/plans/2026-08-17-hf-mjs01-protocol-plan.md
- Spec §8 "Fixtures (OQ9)" (binding): /Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js.docs-superpowers-specs/docs/specs/2026-07-09-ledger-v8-v9-dual-support-design.md

Needs the spike repo (github.com/shieldedtech/spike-dapp-hf). If unreachable, STOP and report.

Workflow: NEW wt worktree from clean origin/main, branch feat/1004-hf-fixtures
(wt switch --create feat/1004-hf-fixtures --no-cd; EnterWorktree; yarn install
&& yarn build before first push). Use superpowers:subagent-driven-development
if you delegate; TDD; Apache headers; conventional commits; do not push or
open a PR without user approval.

Deliverable: testkit-js/testkit-js/src/fixtures/hf/ with EXACTLY these names
(consumed verbatim by later engine tests):
  state-v8.hex, state-v8-v6-envelope.hex, state-migrated-v9.hex,
  state-migrated-v9-merkle.hex, state-tampered-keyset-v8to9.hex,
  state-tampered-keyset-v9to8.hex, state-tampered-bytes.hex,
  state-both-keys.hex, state-co-v2-only-foreign.hex,
  twin-contract/ (v9-compiled twin of the minimal fixture contract),
  fixtures.json (each fixture's protocolVersion int).
Prefer minting via ported spike generators (ledger-v8 + onchain-runtime-v3 as
testkit devDependencies; migrated-state via the spike's migrate flow); commit
golden hex where minting is expensive. Minimal-size mandate: the spike's
counter contract (repo precedent: WASM-fixture coverage timeouts — see
docs/plans plan Global Constraints). Add a generator README (how each fixture
was minted, from which spike island). A smoke test asserts every fixture file
parses/loads and fixtures.json matches the file list (strict equality).

Also record (as a draft note file, not a GitHub post): the verification-
harness decision input (ledger-v9 local verify vs ported spike simulator) and
the three harness-gated security negatives that the OQ9 close must rule on
(perturbed-bytes apply, cross-era rewrap, double-submit).

Hard rules: never cite spec sections (OQ/§/D) in code or comments; error
codes prefix MIDNIGHT_JS_*; no `any`/`as unknown` (cast gate is
no-new-occurrences vs scripts/cast-baseline.txt).
