TASK: OQ13 discovery spike for the HF v8/v9 dual-support work (midnight-js).
This is Task 0.1 of the MJS-01 plan. Read first:
- Plan: /Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js.docs-superpowers-specs/docs/plans/2026-08-17-hf-mjs01-protocol-plan.md (Task 0.1 + §4.1(4))
- Spec (binding authority): /Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js.docs-superpowers-specs/docs/superpowers/specs/2026-07-09-ledger-v8-v9-dual-support-design.md (§4.1(4), D11, D15, OQ13, A4, A5)

You need access to the spike repo (github.com/shieldedtech/spike-dapp-hf) and a
REAL 0.16-generated contract artifact (compact 0.31.1 / compact-runtime 0.16.0
toolchain — see the repo's .envrc bump procedure if you must generate one).
If either is unreachable, STOP and report what is missing.

Deliverables (answer all five; each becomes a spec/OQ13 update + a DRAFT
ticket comment saved to files — you never post to GitHub yourself):
1. 0.16-runtime acquisition mechanism (BLOCKING for engine tasks 1.7/1.8 and
   overload typing 3.2). Candidates: (a) upstream publishes an era-suffixed
   name; (b) documented npm alias every dApp declares; (c) runtime handle
   extractable from the contract object itself. Decide by EXPERIMENT: inspect
   a real 0.16 artifact — does it carry a usable runtime reference? Prove the
   chosen mechanism with a runnable snippet resolving the exact instance the
   artifact imports. Note: (c) is only acceptable if extractable (else it
   breaks the bump-only DX promise); (b) triggers the OQ2 supply-chain
   extension (alias literal in the migration guide is the control).
2. D11 engine placement: protocol vs internal-to-contracts — write up both
   with the peer-forwarding implications; the owner decides (present, don't
   choose silently).
3. The era-tag field: which property on a 0.16 vs 0.18 contract object
   discriminates pipeline era (exact property path + a type-level
   discriminator usable in overloads).
4. A4 + A5 upstream questions (draft for the tkerber thread on issue #1005):
   does every v9-era deploy populate v3/ir? are fresh v9-era ZKIR-v2 deploys
   sanctioned? is the proof binding envelope/era-specific (A5 — the rewrap
   defence)?
5. Engine leg shape: confirm raw createCircuitContext + invoke (no compact-js)
   against the spike, or pin a 0.16-compatible shim; record the exact call
   sequence.

Rules: read-only vs midnight-js main; write only draft files (put them in a
scratch dir and list paths in your final message); never post to GitHub; never
cite spec section numbers in anything destined for code. Report findings in
plain language with evidence (file:line, command output).
