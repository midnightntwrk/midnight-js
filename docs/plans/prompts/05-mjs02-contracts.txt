TASK: Execute Plan 3 — MJS-02 `contracts` unified entries & dispatch (issue #1005).

BASE (this replaces the old five-item prerequisite list): branch off
`origin/feat/1004-protocol-dual-ledger` — the #1218 integration branch — and
open every MJS-02 PR back onto it, continuing the existing stack. **#1218 does
not have to reach `main` first.** That branch already carries the whole MJS-01
stack (#1155 → #1156 → #1164 → #1168 → #1165 → #1194 → #1198) and the #1006
half (#1204 → #1177 → #1207), so everything the earlier version of this prompt
listed separately — #1155/#1156, the Plan-2 D14-B PR, the cross-plan
provider-members PR, the engine core from prompt 03, and the OQ13
era-tag/overload-typing inputs from prompt 01 — is already in the tree you
branch from. Verify the tip live before starting; it still moves.

STACK DISCIPLINE (learned on the MJS-01 stack, do not rediscover it): sync
parent → child by **merge**, never rebase — rebasing orphans the merge bases
the open PRs rest on. If the root `resolutions` pin moves, every stacked child
needs its own `yarn.lock` commit or CI fails with YN0028. Merge, do not squash,
anything the still-open children descend from.

Invoke superpowers:subagent-driven-development with the plan:
/Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js/.worktrees/docs-superpowers-specs/docs/plans/2026-08-17-hf-mjs02-contracts-plan.md
Spec (binding, v5.4):
/Users/paluchs/iohk/dev/github/midnightntwrk/midnight-js/.worktrees/docs-superpowers-specs/docs/specs/2026-07-09-ledger-v8-v9-dual-support-design.md
— read §4.1(5) (the era facade) FIRST, then §4.3 (the three real arm
asymmetries + the partition-context rule), §4.5/OQ21, §6.2, §8 (contracts
slice), and OQ20/OQ21/OQ22.

READ BEFORE ANYTHING ELSE: the two scope-revision blocks at the top of the
plan. #1194/#1198 moved the era-symmetric half of MJS-02 down into `protocol`
— this package no longer composes transactions on either era — and #1204
already shipped part of what this plan consumes.

ORDER: Task 3.0 (NO CODE — the OQ20 ruling on scoped transactions over a v8
head, confirmation of OQ21 option (a), and the OQ22 call on the unpartitioned
cross-contract callee; 3.5 and 3.7 are blocked on it, so it lands first, as a
spec update on the docs branch). Then 3.1 (non-regression baselines: sha256
manifest of untouched test files + golden fixtures for the ENUMERATED
deterministic stages — decoded state, transcript POJO, unproven-call prototype
pre-binding; structural equality post-binding) as a dedicated
no-production-change PR BEFORE any other change in packages/contracts. Then 3.2
(compile-assertion harness + 0.16 overload prototype — the largest single work
item; the era-tag discriminators it needs are already resolved, spec v5.2 §4.3;
negative compile tests that 0.16/0.18 shapes cannot cross-match; neither-shape
diagnostic), 3.3+3.4 (era dispatch + fail-fasts + key-set truth table + SEC-5
byte-match), 3.5 (keep-state + v8-native orchestration), 3.6+3.7 (stale-head
two-step remediation with the deploy branch; scoped-transaction era rules),
3.8 (breadcrumbs + AC2 registry meta-test).

ALREADY ON MAIN — EXTEND, DO NOT RE-CREATE:
`packages/contracts/src/internal/era.ts` (`requireV9`, `requireV9Record`),
`EraInvariantViolationError` and `UntaggedPayloadError`,
`CONTRACTS_ERROR_CODES` with its single member
`MIDNIGHT_JS_C_ERA_INVARIANT_VIOLATION`, the repo-level registry in
`packages/utils/src/error-codes.ts`, and `parseSerializedTag` in
`packages/utils/src/serialized-tag.ts`. New contracts codes go into
`CONTRACTS_ERROR_CODES` next to the existing one; the aggregate picks them up.
Registry hazard: the registry test's expectation list is hand-spelled and has
already gone stale once (protocol grew 6 → 12 codes) — derive it, do not spell
it.

FACTS FROM #1218 THAT CHANGE WHAT YOU WRITE:
- OQ19 is CLOSED. `Ledger8ZswapUnsupportedError` and its code do not exist; the
  protocol registry is 12 codes with `UNKNOWN_LEDGER8_AXIS`. A coin-moving call
  composes on either era, so Task 3.5 carries a required POSITIVE (offer built
  from `TranscriptPojo.zswapLocalState` via `zswapStateToSegmentedOffer`), not
  a refusal negative.
- Error names as shipped: `ComposeFailedError`, `ComposeOptionError`,
  `StateDecodeFailedError`. The `Ledger8*` prefix survives only where the era
  is the subject rather than a parameter.
- The seam union is asymmetric: `VersionedTx<T>` carries
  `{ version: 'v8', txBytes }`, but the READ surface's
  `VersionedFinalizedTxData` carries `tx` on its v8 arm, not `txBytes`. Narrow
  the first with `unwrapV9(payload, seam)`, the second with a plain
  `switch (record.version)`.
- Implementing or mocking a wallet/midnight provider: use `createWalletProvider`
  / `createMidnightProvider` from `types` rather than tagging by hand.
- New ESLint gates: a runtime import of `protocol/v8` OR `protocol/engine` is
  banned outside `packages/protocol/src` — in package sources, package tests and
  testkit alike (type-only imports are allowed). Reach the engine only through
  `loadLedger8Engine()` on the barrel.

NON-NEGOTIABLE BEHAVIORS from review rounds (already in the plan, easy to
miss): stale-head predicate compares ERAS not raw ints; the stale-head error
text itself carries the two-step remediation (verify-no-finalization, then
re-run) and a deploy-specific branch pointing at the runtime-deploy guide
chapter; head/state era mismatch first does a cache-bypassing fresh head read
(`queryLatestProtocolVersion({ fresh: true })`) and raises
indexer-inconsistency (NOT fork-in-progress) if the fresh head still disagrees;
the era-invariant assert on the 0.18 return path throws the already-registered
`MIDNIGHT_JS_C_ERA_INVARIANT_VIOLATION`; mixed-era scopes throw
`MIDNIGHT_JS_C_MIXED_ERA_SCOPE`; wrapped external causes (node submit
rejection, proof-server HTTP) are sanitized before the logger seam.

SOURCES YOU MAY CITE IN CODE (they ship with #1218, so they reach `main` with
the work; the spec and the plan never do): ADR 0004
(lazy v8 era access), ADR 0006 (version-tagged payloads at provider seams),
ADR 0007 (cross the era boundary with plain data only), and the nine package
documents under `packages/protocol/docs/` — `era-seam`,
`compose-refusal-order`, `retained-era-execution`, `fail-closed-decoding`,
`injected-vendor-slices`, `module-graph-and-lazy-loading`,
`shared-table-discipline`, `verifier-keys`, `dual-instantiation-guard`. Read
`compose-refusal-order` and `era-seam` before Task 3.5, and take the closed
14-member `ComposeStage` union off `packages/protocol/src/errors.ts` rather
than guessing it.

Workflow: NEW wt worktree per PR, branched from the current tip of
`origin/feat/1004-protocol-dual-ledger` (prefix feat/1005-*); yarn install &&
yarn build before first push; TDD; SDD loop.
Hard rules: never cite the spec (OQ*, §*, D*, file name) in code, comments or
error messages — cite the ADRs and package documents above instead; codes
MIDNIGHT_JS_C_* from the utils registry, every new code negative-tested
(meta-test asserts registry == tested set); no `any`/`as unknown`; Apache
headers; conventional commits; subagents never push/post; PRs only with user
approval.
